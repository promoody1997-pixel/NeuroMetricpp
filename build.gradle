// Top-level build file
plugins {
    // نستخدم أدوات أندرويد فقط (بدون Kotlin)
    id 'com.android.application' version '8.2.0' apply false
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
